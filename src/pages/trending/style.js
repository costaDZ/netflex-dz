import { makeStyles } from '@material-ui/core';


const useStyles = makeStyles((theme) => {
    return {
        title: {
            textAlign: "center",
            margin: "1em 0",
            fontSize: "2em",
        }
    }
})

export default useStyles;
